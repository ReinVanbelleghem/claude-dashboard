# dependencies
node_modules/

# build output
dist/
build/

# macOS
.DS_Store

# logs
*.log

# local env — the dashboard itself keeps state in ~/.claude-dashboard, well
# outside this tree, but nothing stops a future script from wanting a .env
.env
.env.*
