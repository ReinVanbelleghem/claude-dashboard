<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" role="img" aria-label="Claude Dashboard">
  <!-- Tile uses --series-2 (the palette's clay), so the tab chip matches the app. -->
  <rect width="32" height="32" rx="7" fill="#d95926"/>
  <!-- Prompt chevron + cursor: reads as "a session waiting on you" even at 16px. -->
  <path d="M10 10.5 L15.5 16 L10 21.5" fill="none" stroke="#fff" stroke-width="3.4"
        stroke-linecap="round" stroke-linejoin="round"/>
  <rect x="17.6" y="19.8" width="8.4" height="3.2" rx="1.6" fill="#fff"/>
</svg>
